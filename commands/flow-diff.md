# Slash Command: /flow:diff

## Purpose
Detail differences in nodes, connections, and parameter keys between two workflow JSON files.

## Execution Guide
```bash
flowforge diff <old-filepath> <new-filepath>
```

## Guidance
*   Call out additions, removals, and modifications of nodes, connection routes, and parameter fields clearly.
